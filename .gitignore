# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
build
*.local

# Env files — 시크릿이 들어갈 수 있는 local override 는 절대 커밋하지 않는다.
# .env 는 공개 가능한 기본값만 담고 커밋한다(VITE_* 는 번들에 포함되므로 시크릿 금지).
.env.local
.env.*.local
.env.development.local
.env.production.local

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?
